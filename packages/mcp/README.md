# @bounceless/mcp

Local stdio MCP server for the Bounceless GA single and batch API. Set `BOUNCELESS_API_KEY`; the default API is `https://api.bounceless.io`.

The 1.0 surface is exactly `verify_email`, `verify_batch`, `get_job`, and `get_results`.
