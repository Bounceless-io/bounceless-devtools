# @bounceless/cli

Set `BOUNCELESS_API_KEY`, then use `bounceless verify <email>`, `bounceless batch submit <file.csv>`, `bounceless batch status <request-id>`, or `bounceless batch results <request-id> [--output json|csv]`. No credential is stored locally.
